import React from "react";
import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'Login - Recruitment Placement Global',
    description: 'Login page for Recruitment Placement Global',
}

const Login = () =>{
    return(
        <div>
            <h1>Login</h1>
        </div>
    )
} 

export default Login